{{ define "globals/navbar.tmpl" }}
<section id="navbar">
    <div class="container">
    <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
            <span class="fs-4">Data Entry</span>
        </a>

        <ul class="nav nav-pills">
            <li class="nav-item">
                <a href="/"
                class="nav-link {{ if eq .Page "Beranda" }} active {{ end }}"
                {{ if eq .Page "Beranda" }} aria-current="page" {{ end }}>
                    Beranda
                </a>
            </li>
            <li class="nav-item">
                <a href="/users"
                class="nav-link {{ if eq .Page "Pelanggan" }} active {{ end }}"
                {{ if eq .Page "Pelanggan" }} aria-current="page" {{ end }}>
                    Pelanggan
                </a>
            </li>
        </ul>
    </header>
    </div>
</section>
{{ end }}